# Config Profiles

Place exported slicer profiles here on the host:

- `machine.json`
- `process.json`
- `filaments/generic.json`
- optional exact material profiles such as `filaments/PLA.json`

Profile JSON files are intentionally ignored by git because they are local printer/slicer setup.
